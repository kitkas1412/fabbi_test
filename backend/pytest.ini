[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
filterwarnings =
    error:The event_loop fixture provided by pytest-asyncio has been redefined:pytest.PytestDeprecationWarning
markers =
    integration: requires the Compose PostgreSQL and Redis services
